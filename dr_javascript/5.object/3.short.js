const x = 0;
const y = 0;
const coordinate = { x, y }; // { x: x, y: y }; key이름과 참조하고 있는 변수의 이름이 같을경우 생략가능
console.log(coordinate);

function makeObj(naem, age) {
  return {
    name,
    age,
  };
}
